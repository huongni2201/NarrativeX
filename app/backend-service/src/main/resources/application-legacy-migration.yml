# Opt-in only for a controlled migration of a known legacy schema.
# Never enable this profile for normal development, CI, staging, or production bootstrap.
spring:
  flyway:
    baseline-on-migrate: true
