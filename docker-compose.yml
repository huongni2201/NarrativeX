name: ${COMPOSE_PROJECT_NAME:-narrativex-prod}

x-postgres-environment: &postgres-environment
  DB_HOST: postgres
  DB_PORT: 5432
  DB_NAME: ${DB_NAME:-narrativex}
  DB_USERNAME: ${DB_USERNAME:-narrativex}
  DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}

x-worker-database-environment: &worker-database-environment
  DATABASE_URL: postgresql://${DB_USERNAME:-narrativex}:${DB_PASSWORD:?Set DB_PASSWORD in .env}@postgres:5432/${DB_NAME:-narrativex}

x-build-args: &build-args
  BUILD_SHA: ${BUILD_SHA:-local}

services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-narrativex}
      POSTGRES_USER: ${DB_USERNAME:-narrativex}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
    volumes: [postgres-data:/var/lib/postgresql]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-narrativex} -d ${DB_NAME:-narrativex}"]
      interval: 5s
      timeout: 5s
      retries: 20
    ports: ["127.0.0.1:5432:5432"]
    networks: [appnet]

  backend:
    image: ${BACKEND_IMAGE:-narrativex-backend}:${BUILD_SHA:-local}
    build:
      context: ./app/backend-service
      dockerfile: Dockerfile
      args: *build-args
    restart: unless-stopped
    environment:
      <<: *postgres-environment
      SPRING_PROFILES_ACTIVE: prod
      PORT: 8080
      BUILD_SHA: ${BUILD_SHA:-local}
      NARRATIVEX_SESSION_TIMEOUT: ${NARRATIVEX_SESSION_TIMEOUT:-7d}
      NARRATIVEX_OIDC_ENABLED: ${NARRATIVEX_OIDC_ENABLED:-true}
      NARRATIVEX_LOCAL_DEV_IDENTITY_ENABLED: "false"
      NARRATIVEX_PUBLIC_BASE_URL: ${NARRATIVEX_PUBLIC_BASE_URL:?Set NARRATIVEX_PUBLIC_BASE_URL in .env}
      # Packaged Electron calls the backend through main-process fetch, so production
      # normally has no browser Origin to allow. Set explicit HTTPS origins only when needed.
      NARRATIVEX_CORS_ALLOWED_ORIGINS: "${NARRATIVEX_CORS_ALLOWED_ORIGINS:-}"
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:?Set GOOGLE_OAUTH_CLIENT_ID in .env}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:?Set GOOGLE_OAUTH_CLIENT_SECRET in .env}
      NARRATIVEX_MAX_STORY_CHARACTERS: ${NARRATIVEX_MAX_STORY_CHARACTERS:-500000}
      NARRATIVEX_MAX_ESTIMATED_INPUT_TOKENS: ${NARRATIVEX_MAX_ESTIMATED_INPUT_TOKENS:-120000}
      NARRATIVEX_MAX_IMPORT_FILE_SIZE: ${NARRATIVEX_MAX_IMPORT_FILE_SIZE:-10MB}
      NARRATIVEX_MAX_IMPORT_REQUEST_SIZE: ${NARRATIVEX_MAX_IMPORT_REQUEST_SIZE:-11MB}
      NARRATIVEX_MEDIA_GENERATION_ENABLED: "true"
      NARRATIVEX_IMAGE_PROVIDER_KEY: ${NARRATIVEX_IMAGE_PROVIDER_KEY:-vertex}
      NARRATIVEX_IMAGE_MODEL: ${NARRATIVEX_IMAGE_MODEL:-gemini-2.5-flash-image}
      NARRATIVEX_IMAGE_PRICING_VERSION: ${NARRATIVEX_IMAGE_PRICING_VERSION:-gemini-2.5-flash-image-batch-2026-08-22}
      NARRATIVEX_IMAGE_EXECUTION_MODE: ${NARRATIVEX_IMAGE_EXECUTION_MODE:-batch}
      NARRATIVEX_IMAGE_UNIT_COST_DRAFT: ${NARRATIVEX_IMAGE_UNIT_COST_DRAFT:-0.10}
      NARRATIVEX_IMAGE_UNIT_COST_STANDARD: ${NARRATIVEX_IMAGE_UNIT_COST_STANDARD:-0.25}
      NARRATIVEX_IMAGE_UNIT_COST_HIGH: ${NARRATIVEX_IMAGE_UNIT_COST_HIGH:-0.40}
      NARRATIVEX_STORAGE_UPLOAD_INTENT_TTL: ${NARRATIVEX_STORAGE_UPLOAD_INTENT_TTL:-15m}
      VERTEX_GEMINI_ENABLED: "true"
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT:?Set GOOGLE_CLOUD_PROJECT in .env}
      VERTEX_LOCATION: ${VERTEX_LOCATION:-us-central1}
      VERTEX_GEMINI_MODEL: ${VERTEX_GEMINI_MODEL:-gemini-2.5-flash}
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID:?Set R2_ACCOUNT_ID in .env}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID:?Set R2_ACCESS_KEY_ID in .env}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY:?Set R2_SECRET_ACCESS_KEY in .env}
      R2_BUCKET: ${R2_BUCKET:?Set R2_BUCKET in .env}
      R2_ENDPOINT: ${R2_ENDPOINT:-}
    # Desktop development and the system-browser OAuth handoff run on the host,
    # so the backend must be reachable at http://localhost:8080. Keep the bind
    # loopback-only; worker services continue using http://backend:8080.
    ports: ["127.0.0.1:8080:8080"]
    depends_on:
      postgres: { condition: service_healthy }
    networks: [appnet]

  ai-worker:
    image: ${AI_WORKER_IMAGE:-narrativex-ai-worker}:${BUILD_SHA:-local}
    build:
      context: ./app/ai-worker
      dockerfile: Dockerfile
      target: worker-image
      args: *build-args
    restart: unless-stopped
    environment:
      <<: *worker-database-environment
      WORKER_NAME: narrativex-prod-ai
      WORKER_ENV: production
      WORKER_ROLES: analysis,media-validation,image-generation
      LOG_LEVEL: ${WORKER_LOG_LEVEL:-INFO}
      BACKEND_URL: http://backend:8080
      BUILD_SHA: ${BUILD_SHA:-local}
      POLL_INTERVAL_SECONDS: ${WORKER_POLL_INTERVAL_SECONDS:-1}
      LEASE_SECONDS: ${WORKER_LEASE_SECONDS:-60}
      WORKER_CONCURRENCY: ${GENERAL_WORKER_CONCURRENCY:-4}
      MEDIA_DOWNLOAD_TIMEOUT_SECONDS: ${MEDIA_DOWNLOAD_TIMEOUT_SECONDS:-120}
      MEDIA_PROBE_TIMEOUT_SECONDS: ${MEDIA_PROBE_TIMEOUT_SECONDS:-30}
      MEDIA_MAX_AUDIO_BYTES: ${MEDIA_MAX_AUDIO_BYTES:-104857600}
      MEDIA_MAX_IMAGE_BYTES: ${MEDIA_MAX_IMAGE_BYTES:-104857600}
      MEDIA_MAX_VIDEO_BYTES: ${MEDIA_MAX_VIDEO_BYTES:-1073741824}
      AI_PROVIDER_MODE: vertex
      VERTEX_PROJECT_ID: ${GOOGLE_CLOUD_PROJECT:?Set GOOGLE_CLOUD_PROJECT in .env}
      VERTEX_LOCATION: ${VERTEX_LOCATION:-us-central1}
      VERTEX_MODEL: ${VERTEX_GEMINI_MODEL:-gemini-2.5-flash}
      VERTEX_TIMEOUT_SECONDS: ${VERTEX_TIMEOUT_SECONDS:-120}
      IMAGE_PROVIDER_MODE: vertex
      VERTEX_IMAGE_MODEL: ${NARRATIVEX_IMAGE_MODEL:-gemini-2.5-flash-image}
      VERTEX_IMAGE_LOCATION: ${VERTEX_IMAGE_LOCATION:-global}
      VERTEX_IMAGE_TIMEOUT_SECONDS: ${VERTEX_IMAGE_TIMEOUT_SECONDS:-120}
      VERTEX_IMAGE_SERVICE_TIER: standard
      VERTEX_IMAGE_BATCH_MAX_ITEMS: ${VERTEX_IMAGE_BATCH_MAX_ITEMS:-50}
      VERTEX_IMAGE_BATCH_LOCATION: ${VERTEX_IMAGE_BATCH_LOCATION:-global}
      VERTEX_IMAGE_BATCH_GCS_BUCKET: ${VERTEX_IMAGE_BATCH_GCS_BUCKET:?Set VERTEX_IMAGE_BATCH_GCS_BUCKET in .env}
      VERTEX_IMAGE_BATCH_GCS_PREFIX: ${VERTEX_IMAGE_BATCH_GCS_PREFIX:-narrativex/image-batches}
      VERTEX_IMAGE_BATCH_POLL_SECONDS: ${VERTEX_IMAGE_BATCH_POLL_SECONDS:-30}
      VERTEX_IMAGE_BATCH_HTTP_TIMEOUT_SECONDS: ${VERTEX_IMAGE_BATCH_HTTP_TIMEOUT_SECONDS:-120}
      VERTEX_IMAGE_UNKNOWN_MAX_AGE_SECONDS: ${VERTEX_IMAGE_UNKNOWN_MAX_AGE_SECONDS:-3600}
      IMAGE_RECONCILE_MAX_ATTEMPTS: ${IMAGE_RECONCILE_MAX_ATTEMPTS:-5}
      IMAGE_CIRCUIT_BREAKER_FAILURE_THRESHOLD: ${IMAGE_CIRCUIT_BREAKER_FAILURE_THRESHOLD:-3}
      IMAGE_CIRCUIT_BREAKER_OPEN_SECONDS: ${IMAGE_CIRCUIT_BREAKER_OPEN_SECONDS:-120}
      IMAGE_MAX_OUTPUT_BYTES: ${IMAGE_MAX_OUTPUT_BYTES:-15000000}
      TTS_PROVIDER_MODE: disabled
      MEDIA_STORAGE_MODE: r2
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID:?Set R2_ACCOUNT_ID in .env}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID:?Set R2_ACCESS_KEY_ID in .env}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY:?Set R2_SECRET_ACCESS_KEY in .env}
      R2_BUCKET: ${R2_BUCKET:?Set R2_BUCKET in .env}
      R2_ENDPOINT: ${R2_ENDPOINT:-}
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/gcp-service-account.json
    volumes:
      - type: bind
        source: ${GCP_SERVICE_ACCOUNT_FILE:?Set GCP_SERVICE_ACCOUNT_FILE in .env}
        target: /run/secrets/gcp-service-account.json
        read_only: true
        bind:
          create_host_path: false
    depends_on:
      backend: { condition: service_healthy }
      postgres: { condition: service_healthy }
    networks: [appnet]

  narration-worker:
    image: ${NARRATION_WORKER_IMAGE:-narrativex-narration-worker}:${BUILD_SHA:-local}
    build:
      context: ./app/ai-worker
      dockerfile: Dockerfile
      target: worker-narration
      args: *build-args
    restart: unless-stopped
    environment:
      <<: *worker-database-environment
      WORKER_NAME: narrativex-prod-narration
      WORKER_ENV: production
      WORKER_ROLES: narration
      LOG_LEVEL: ${WORKER_LOG_LEVEL:-INFO}
      BACKEND_URL: http://backend:8080
      BUILD_SHA: ${BUILD_SHA:-local}
      POLL_INTERVAL_SECONDS: ${WORKER_POLL_INTERVAL_SECONDS:-1}
      LEASE_SECONDS: ${WORKER_LEASE_SECONDS:-60}
      WORKER_CONCURRENCY: ${NARRATION_WORKER_CONCURRENCY:-1}
      MEDIA_DOWNLOAD_TIMEOUT_SECONDS: ${MEDIA_DOWNLOAD_TIMEOUT_SECONDS:-120}
      MEDIA_PROBE_TIMEOUT_SECONDS: ${MEDIA_PROBE_TIMEOUT_SECONDS:-30}
      MEDIA_MAX_AUDIO_BYTES: ${MEDIA_MAX_AUDIO_BYTES:-104857600}
      MEDIA_MAX_IMAGE_BYTES: ${MEDIA_MAX_IMAGE_BYTES:-104857600}
      MEDIA_MAX_VIDEO_BYTES: ${MEDIA_MAX_VIDEO_BYTES:-1073741824}
      AI_PROVIDER_MODE: disabled
      IMAGE_PROVIDER_MODE: disabled
      TTS_PROVIDER_MODE: vieneu
      NARRATION_MP3_BITRATE: ${NARRATION_MP3_BITRATE:-96k}
      VIENEU_VOICE_ID: ${VIENEU_VOICE_ID:-vieneu-ngoc-huyen-v2}
      VIENEU_VOICE_NAME: ${VIENEU_VOICE_NAME:-Ngọc Huyền v2}
      # Ngọc Huyền v2 is an enrolled reference voice, not a VieNeu built-in preset.
      VIENEU_REFERENCE_AUDIO_PATH: /run/narrativex/voices/reference.wav
      VIENEU_BACKEND: ${VIENEU_BACKEND:-onnx}
      VIENEU_PRECISION: ${VIENEU_PRECISION:-int8}
      VIENEU_THREADS: ${VIENEU_THREADS:-0}
      VIENEU_BATCH_MAX_SEGMENTS: ${VIENEU_BATCH_MAX_SEGMENTS:-8}
      VIENEU_MAX_BATCH_SIZE: ${VIENEU_MAX_BATCH_SIZE:-32}
      VIENEU_INFERENCE_CONCURRENCY: ${VIENEU_INFERENCE_CONCURRENCY:-1}
      VIENEU_DENOISE_REFERENCE: ${VIENEU_DENOISE_REFERENCE:-true}
      VIENEU_SAVE_VOICE_PROFILE: ${VIENEU_SAVE_VOICE_PROFILE:-false}
      VIENEU_FORCE_REENROLL: ${VIENEU_FORCE_REENROLL:-false}
      VIENEU_APPLY_WATERMARK: ${VIENEU_APPLY_WATERMARK:-false}
      MEDIA_STORAGE_MODE: r2
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID:?Set R2_ACCOUNT_ID in .env}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID:?Set R2_ACCESS_KEY_ID in .env}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY:?Set R2_SECRET_ACCESS_KEY in .env}
      R2_BUCKET: ${R2_BUCKET:?Set R2_BUCKET in .env}
      R2_ENDPOINT: ${R2_ENDPOINT:-}
    volumes:
      - type: bind
        source: ${VIENEU_REFERENCE_AUDIO_FILE:?Set VIENEU_REFERENCE_AUDIO_FILE in .env for Ngọc Huyền v2}
        target: /run/narrativex/voices/reference.wav
        read_only: true
        bind:
          create_host_path: false
      - vieneu-home:/home/appuser
    depends_on:
      backend: { condition: service_healthy }
      postgres: { condition: service_healthy }
    networks: [appnet]

  cloudflared:
    profiles: ["tunnel"]
    image: ${CLOUDFLARED_IMAGE:-cloudflare/cloudflared:2026.8.2}
    restart: unless-stopped
    command: ["tunnel", "--no-autoupdate", "run"]
    environment:
      # Do not use ${VAR:?} here: Compose interpolates inactive profiles too.
      # If the tunnel profile is enabled without a token, cloudflared fails closed.
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:-}
    depends_on:
      backend: { condition: service_healthy }
    networks: [appnet]

networks:
  appnet: { driver: bridge }
volumes:
  postgres-data:
  vieneu-home:
