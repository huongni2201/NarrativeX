<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.narrativex.backend.feature.generation.infrastructure.persistence.mybatis.GenerationOutboxMapper">
    <insert id="enqueue"
            parameterType="com.narrativex.backend.feature.generation.infrastructure.persistence.mybatis.GenerationOutboxRow">
        INSERT INTO outbox_events
          (aggregate_type, aggregate_id, event_type, event_key, payload_json, status)
        VALUES
          (
            'GENERATION_JOB',
            #{aggregateId},
            'GENERATION_JOB_QUEUED',
            #{eventKey},
            jsonb_build_object(
              'jobId', #{aggregateId},
              'jobType', #{jobType},
              'projectId', #{projectId},
              'storyVersionId', CAST(#{storyVersionId,jdbcType=BIGINT} AS BIGINT),
              'chapterId', CAST(#{chapterId,jdbcType=BIGINT} AS BIGINT),
              'chapterRowVersion', CAST(#{chapterRowVersion,jdbcType=BIGINT} AS BIGINT),
              'sourceHash', #{sourceHash,jdbcType=VARCHAR},
              'contentVariantId', CAST(#{contentVariantId,jdbcType=BIGINT} AS BIGINT),
              'sourceVariantId', CAST(#{sourceVariantId,jdbcType=BIGINT} AS BIGINT),
              'targetLanguage', #{targetLanguage,jdbcType=VARCHAR},
              'mediaPlanId', CAST(#{mediaPlanId,jdbcType=OTHER,typeHandler=com.narrativex.backend.feature.generation.infrastructure.persistence.mybatis.UuidTypeHandler} AS UUID),
              'mediaPlanRevision', CAST(#{mediaPlanRevision,jdbcType=INTEGER} AS INTEGER),
              'productionMode', CAST(#{productionMode,jdbcType=VARCHAR} AS TEXT)
            ),
            'PENDING'
          )
        ON CONFLICT (event_key) DO NOTHING
    </insert>
    <select id="reserveBatch" resultType="com.narrativex.backend.feature.generation.infrastructure.persistence.mybatis.OutboxDispatchRow">
        WITH candidates AS (
            SELECT id FROM outbox_events
             WHERE status = 'PENDING'
               AND event_type IN ('GENERATION_JOB_QUEUED', 'MEDIA_VALIDATION_REQUESTED')
               AND available_at &lt;= CURRENT_TIMESTAMP
             ORDER BY id LIMIT 50 FOR UPDATE SKIP LOCKED
        )
        UPDATE outbox_events event
           SET available_at = CURRENT_TIMESTAMP + (#{reservationMillis} * INTERVAL '1 millisecond')
          FROM candidates
         WHERE event.id = candidates.id
        RETURNING event.id, event.event_type, event.payload_json::text AS payload_json
    </select>

    <update id="markPublished">
        UPDATE outbox_events SET status = 'PUBLISHED', attempts = attempts + 1
         WHERE id = #{id} AND status = 'PENDING'
    </update>

    <update id="scheduleRetry">
        UPDATE outbox_events
           SET attempts = attempts + 1,
               available_at = CURRENT_TIMESTAMP + (#{retryMillis} * INTERVAL '1 millisecond')
         WHERE id = #{id} AND status = 'PENDING'
    </update>
</mapper>
