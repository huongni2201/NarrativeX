<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.narrativex.backend.feature.render.infrastructure.persistence.mybatis.FinalArtifactMapper">
    <resultMap id="finalArtifactRow" type="com.narrativex.backend.feature.render.infrastructure.persistence.mybatis.FinalArtifactRow">
        <id property="id" column="id"/>
        <result property="projectId" column="project_id"/>
        <result property="chapterId" column="chapter_id"/>
        <result property="artifactType" column="artifact_type"/>
        <result property="renderFingerprint" column="render_fingerprint"/>
        <result property="storageKey" column="storage_key"/>
        <result property="mimeType" column="mime_type"/>
        <result property="sizeBytes" column="size_bytes"/>
        <result property="checksumSha256" column="checksum_sha256"/>
        <result property="durationMs" column="duration_ms"/>
        <result property="width" column="width"/>
        <result property="height" column="height"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    <select id="findOwned" resultMap="finalArtifactRow">
        SELECT fa.id, fa.project_id, fa.chapter_id, fa.artifact_type, fa.render_fingerprint,
               fa.storage_key,
               fa.mime_type, fa.size_bytes, fa.checksum_sha256, fa.duration_ms,
               fa.width, fa.height, fa.status, fa.created_at, fa.updated_at
          FROM final_artifacts fa
          JOIN projects p ON p.id = fa.project_id
         WHERE fa.id = #{artifactId}
           AND p.owner_id = #{ownerId}
           AND fa.status = 'READY'
    </select>

    <select id="findByGenerationJobId" resultMap="finalArtifactRow">
        SELECT fa.*
          FROM final_artifacts fa
          JOIN generation_jobs gj ON gj.id = fa.generation_job_id
         WHERE gj.job_id = CAST(#{jobId} AS UUID)
           AND fa.status = 'READY'
         LIMIT 1
    </select>
</mapper>
