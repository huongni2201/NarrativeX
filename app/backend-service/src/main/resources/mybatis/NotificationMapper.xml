<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.narrativex.backend.feature.notification.infrastructure.persistence.mybatis.NotificationMapper">
    <resultMap id="notificationRow" type="com.narrativex.backend.feature.notification.infrastructure.persistence.mybatis.NotificationRow">
        <id property="id" column="id" />
        <result property="projectId" column="project_id" />
        <result property="type" column="type" />
        <result property="titleKey" column="title_key" />
        <result property="messageKey" column="message_key" />
        <result property="readAt" column="read_at" />
        <result property="createdAt" column="created_at" />
    </resultMap>
    <sql id="columns">id, project_id, type, title_key, message_key, read_at, created_at</sql>
    <select id="list" resultMap="notificationRow">
        SELECT <include refid="columns" /> FROM notifications
         WHERE user_id = #{userId}
           AND (#{unreadOnly} = FALSE OR read_at IS NULL)
         ORDER BY created_at DESC, id DESC
         LIMIT #{limit}
    </select>
    <select id="unreadCount" resultType="int">
        SELECT COUNT(*)::int FROM notifications WHERE user_id = #{userId} AND read_at IS NULL
    </select>
    <select id="markRead" resultMap="notificationRow" affectData="true">
        UPDATE notifications SET read_at = COALESCE(read_at, CURRENT_TIMESTAMP)
         WHERE id = #{notificationId} AND user_id = #{userId}
         RETURNING <include refid="columns" />
    </select>
    <update id="markAllRead">
        UPDATE notifications SET read_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId} AND read_at IS NULL
    </update>
</mapper>
