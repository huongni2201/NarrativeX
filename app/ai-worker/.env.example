# Worker Environment Configuration
WORKER_NAME=narrativex-worker
WORKER_ENV=development
# Host only the roles this process should execute. Use WORKER_ROLES=narration for a dedicated TTS worker.
WORKER_ROLES=analysis,translation,narration,media-validation,image-generation
LOG_LEVEL=INFO
BACKEND_URL=http://localhost:8080
HEALTH_CHECK_PORT=8001
DATABASE_URL=postgresql://narrativex:change-me@localhost:5432/narrativex
POLL_INTERVAL_SECONDS=1
LEASE_SECONDS=60
WORKER_CONCURRENCY=4
MEDIA_DOWNLOAD_TIMEOUT_SECONDS=120
MEDIA_PROBE_TIMEOUT_SECONDS=30
MEDIA_MAX_AUDIO_BYTES=104857600
MEDIA_MAX_IMAGE_BYTES=104857600
MEDIA_MAX_VIDEO_BYTES=1073741824

# Story-analysis provider configuration; credentials come from ADC/workload identity.
AI_PROVIDER_MODE=disabled
# CI/E2E may use AI_PROVIDER_MODE=fake for deterministic scene/visual-beat analysis.
VERTEX_PROJECT_ID=
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-2.5-flash
VERTEX_TIMEOUT_SECONDS=120
# In the production-profile Docker runtime this path is backed by the read-only host gcloud mount in docker-compose.prod.yml.
GOOGLE_APPLICATION_CREDENTIALS=/home/appuser/.config/gcloud/application_default_credentials.json

# Full-chapter narration uses local VieNeu-TTS; durable final audio is stored in Cloudflare R2.
TTS_PROVIDER_MODE=disabled
# CI/E2E may use TTS_PROVIDER_MODE=fake; it still assembles and probes real FFmpeg audio.
TTS_PRICING_CATALOG_VERSION=vieneu-local-2026-08-23
NARRATION_MP3_BITRATE=96k
# VieNeu-TTS v3 Turbo / Instant Voice Cloning. CPU uses the torch-free ONNX runtime by default.
VIENEU_VOICE_ID=vieneu-ngoc-huyen-v2
VIENEU_VOICE_NAME=Ngọc Huyền v2
# Built-in preset alternative: vieneu-ngoc-huyen / Ngọc Huyền (no reference WAV required).
# Path inside the worker container. When using docker-compose, mount the host
# directory containing this file at /run/narrativex/voices.
VIENEU_REFERENCE_AUDIO_PATH=/run/narrativex/voices/ngoc_huyen_sample.wav
VIENEU_BACKEND=auto
VIENEU_PRECISION=int8
VIENEU_THREADS=0
# NarrativeX segments per VieNeu infer_batch call and VieNeu's internal GPU chunk batch ceiling.
VIENEU_BATCH_MAX_SEGMENTS=8
VIENEU_MAX_BATCH_SIZE=32
# Keep one inference lane per model instance; scale with worker processes/GPUs instead.
VIENEU_INFERENCE_CONCURRENCY=1
VIENEU_DENOISE_REFERENCE=true
# Do not write voice profiles back into site-packages in production containers.
VIENEU_SAVE_VOICE_PROFILE=false
VIENEU_FORCE_REENROLL=false
VIENEU_APPLY_WATERMARK=false
MEDIA_STORAGE_MODE=disabled
# Local E2E mode: MEDIA_STORAGE_MODE=local and MEDIA_LOCAL_DIR=/tmp/narrativex-e2e/media.
MEDIA_LOCAL_DIR=/tmp/narrativex-e2e/media

# Gemini 2.5 Flash Image on Vertex AI.
# All enabled image generation is routed through Vertex Batch inference, including a single image.
# A GCS bucket is mandatory when IMAGE_PROVIDER_MODE=vertex. GCS is staging only; final media is R2.
# gemini-2.5-flash-image does not support Flex PayGo, so keep the service tier standard.
IMAGE_PROVIDER_MODE=disabled
# CI/E2E may use IMAGE_PROVIDER_MODE=fake; only the external image provider is replaced.
FINAL_VIDEO_STORAGE_MODE=google-drive
FINAL_VIDEO_LOCAL_DIR=/tmp/narrativex-e2e/final
VERTEX_IMAGE_MODEL=gemini-2.5-flash-image
VERTEX_IMAGE_LOCATION=global
VERTEX_IMAGE_TIMEOUT_SECONDS=120
VERTEX_IMAGE_SERVICE_TIER=standard
VERTEX_IMAGE_EXECUTION_MODE=batch
VERTEX_IMAGE_BATCH_MAX_ITEMS=50
VERTEX_IMAGE_BATCH_LOCATION=global
VERTEX_IMAGE_BATCH_GCS_BUCKET=
VERTEX_IMAGE_BATCH_GCS_PREFIX=narrativex/image-batches
VERTEX_IMAGE_BATCH_POLL_SECONDS=30
VERTEX_IMAGE_BATCH_HTTP_TIMEOUT_SECONDS=120
VERTEX_IMAGE_UNKNOWN_MAX_AGE_SECONDS=3600
IMAGE_MAX_OUTPUT_BYTES=10485760

# Optional private Wan-compatible video adapter.
WAN_VIDEO_ENABLED=false
WAN_ENDPOINT_URL=
WAN_MODEL=Wan2.2-TI2V-5B
WAN_API_TOKEN=
WAN_REQUEST_TIMEOUT_SECONDS=30

# Cloudflare R2 durable media storage.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=narrativex-dev
R2_ENDPOINT=
