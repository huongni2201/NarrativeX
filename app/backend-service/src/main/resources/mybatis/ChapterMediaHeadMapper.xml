<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.narrativex.backend.feature.generation.infrastructure.persistence.mybatis.ChapterMediaHeadMapper">
    <insert id="upsert">
        INSERT INTO chapter_media_heads (chapter_id, generation_job_id, updated_at)
        VALUES (#{chapterId}, #{generationJobId}, CURRENT_TIMESTAMP)
        ON CONFLICT (chapter_id) DO UPDATE
        SET generation_job_id = EXCLUDED.generation_job_id,
            updated_at = CURRENT_TIMESTAMP
    </insert>
</mapper>
