# Rà soát source, migration và Desktop — 2026-09-08

Phạm vi: trạng thái hiện tại tại HEAD `69964b72e`, worktree ban đầu sạch. Rà soát backend, worker, Desktop, shared contracts, baseline và tài liệu. Không chỉnh implementation, không thay đổi database, không gọi provider trả phí. Báo cáo này là kiểm tra có trọng tâm, không chứng nhận mọi file/luồng đều sạch.

## Kết luận

Chưa nên coi source hiện tại là sẵn sàng phát hành. Desktop không type-check/build được. Có lỗ hổng trong enforcement entitlement, race ở immutable character references, lỗi phục hồi local storage và đồng bộ metadata project. Migration chính đã đi đúng hướng nhưng còn schema/tài liệu chưa dọn và một số ranh giới lớp bị vi phạm.

## Bug và rủi ro có bằng chứng

### 1. P1 — JSX lỗi làm Desktop không build được

- `app/desktop/src/renderer/features/storyboard/components/VisualBeatGrid.tsx:206` thiếu dấu `}` kết thúc biểu thức conditional icon trước `</Button>`.
- `npm run type-check`: `TS1005: '}' expected` tại dòng 207. `npm run build` cũng thất bại ở cùng đoạn JSX.
- Router import screen này theo chuỗi import tĩnh nên đây là lỗi chặn bundle renderer, không chỉ một nút trên Storyboard.
- Sửa syntax rồi chạy lại type-check/build; hiện compiler chưa đi tiếp nên không thể khẳng định không còn lỗi type khác.
- Nút Approve vẫn tồn tại tại dòng 181; không coi nhánh Generate ở dòng 205 là mất chức năng Approve.

### 2. P1 — Export chưa áp dụng đầy đủ entitlement server

- `app/backend-service/src/main/java/com/narrativex/backend/feature/generation/application/usecase/CreateProjectRenderUseCase.java:85` kiểm tra quality và reserve concurrency tại dòng 109, nhưng không enforce/reserve monthly exports.
- `app/backend-service/src/main/java/com/narrativex/backend/feature/generation/application/render/ProjectRenderProfileFactory.java:9` tạo profile video/color/subtitles, không đưa `watermarkRequired` vào snapshot render.
- Seed NORMAL tại `app/backend-service/src/main/resources/db/migration/V8__seed_catalog.sql:18` có watermark và giới hạn 2 longform exports/tháng. Trong các runtime source đã rà soát, monthly export counter/watermark chỉ được khai báo/đọc, không được áp dụng vào luồng xuất.
- Hệ quả từ control flow: user NORMAL có đủ media/timing có thể tiếp tục export vượt hạn mức và profile không buộc watermark. Chưa tái hiện bằng tài khoản/DB thật.
- Đây là policy chưa nối hoàn chỉnh; không xóa các field quota vì tưởng là field thừa. Cần admission/settlement idempotent và render snapshot chứa policy đã được server chốt.

### 3. P1 — Local storage có thể nhận audio ghi dở là audio hoàn chỉnh

- `app/ai-worker/src/narrativex_worker/narration/storage.py:202` ghi trực tiếp vào key chính thức; chưa có staging + atomic publication và metadata integrity gốc được lưu bền vững.
- `find()` tại dòng 212 tính checksum mới từ file đang tồn tại, kể cả file ghi dở.
- `app/ai-worker/src/narrativex_worker/narration/local_runner.py:452` chấp nhận PCM có số byte dương/chẵn và suy thời lượng từ số byte đó.
- Fault reproduction với implementation thật: file PCM dở 9.600 bytes được `_load_existing_local_segment` chấp nhận thành segment 100 ms cho cả câu nguồn. Một reproduction khác để lại 4 bytes rồi retry gặp `MediaAssetConflictError`.
- Hệ quả: crash giữa lúc ghi có thể làm narration bị cắt ngắn âm thầm hoặc job không phục hồi được. Cần atomic write và kiểm tra completion/integrity từ metadata gốc; test filesystem fault thay vì chỉ fake storage.

### 4. P2 — Race làm đổi reference của CharacterVersion đã LOCKED

- `app/backend-service/src/main/java/com/narrativex/backend/feature/character/application/usecase/SetCharacterVersionReferencesUseCase.java:38` đọc/check trạng thái rồi replace references ở dòng 100.
- `app/backend-service/src/main/resources/mybatis/CharacterMapper.xml:32` dùng SELECT thường. `CharacterVersionReferenceMapper.xml:20` DELETE không guard trạng thái/version parent.
- `LockCharacterVersionUseCase.java:26` là luồng lock liên quan. Không tìm thấy DB guard đủ để serialize hai thao tác này.
- Interleaving: A đọc REVIEW → B LOCK và commit → A replace/delete references. Version đã khóa bị thay nội dung identity reference.
- Kết luận từ transaction/control flow, chưa chạy concurrency test trên PostgreSQL. Cần khóa cùng parent row cho cả replace và lock hoặc một cơ chế optimistic concurrency tương đương.

### 5. P2 — Image replay làm mất MIME gốc

- `app/ai-worker/src/narrativex_worker/image_generation_runner.py:170` lưu ảnh dưới key không có extension `private/provider-results/images/<checksum>`.
- `narration/storage.py:202` trả object qua `find()` nếu file đã có; `_mime_for_path()` tại dòng 270 trả `application/octet-stream` cho key này.
- `image_generation_repository/materialization.py:70` persist MIME đó vào `media_assets.content_type`.
- Đã reproduce bằng `LocalMediaStorage`: lần đầu `image/png`, replay cùng bytes thành `application/octet-stream`.
- Trigger: crash sau ghi file trước commit DB, hoặc kết quả khác trùng bytes. Cần lưu/khôi phục metadata MIME và kiểm tra idempotent recovery.

### 6. P2 — Favorite project không cập nhật snapshot mà danh sách đọc

- `app/desktop/src/renderer/features/projects/queries/projects.queries.ts:61` toggle favorite chỉ ghi API rồi invalidate.
- Cùng file, dòng 113–126 lấy danh sách chỉ từ local catalog, không fetch metadata backend và không cập nhật `isStarred` trong snapshot.
- `app/desktop/src/renderer/features/projects/screens/ProjectsScreen.tsx:183` lấy chính `project.isStarred` để quyết định lần click tiếp theo.
- `localProjects.upsert()` trong renderer chỉ có ở luồng create. Invalidation vì vậy vẫn trả trạng thái favorite cũ; click tiếp có thể tiếp tục gọi add thay vì remove.
- Cần cập nhật metadata của các project đã đăng ký local sau mutation, giữ nguyên nguyên tắc discovery device-local. Không cần khôi phục cloud project synchronization.
- Kết luận từ luồng dữ liệu; chưa thao tác UI thật do renderer chưa chạy được.

### 7. P2 — Retry idempotent bị chặn bởi trạng thái hiện tại

- `app/backend-service/src/main/java/com/narrativex/backend/feature/generation/application/usecase/CreateRegenerationJobUseCase.java:75` kiểm tra expiry/source/continuity trước lookup job theo idempotency key ở dòng 86.
- Nếu POST đã commit nhưng response mất, retry cùng key sau TTL hoặc sau sửa source trả `REGENERATION_PLAN_STALE`, thay vì trả job đã nhận.
- `CreateProjectRenderUseCase.java:67` có vấn đề cùng loại: kiểm tra device/timeline hiện tại trước nhánh replay ở dòng 94.
- Nên lookup và xác minh scope/request identity của accepted request trước admission dành cho một job mới. Không bỏ kiểm tra ownership hay cho phép key tái sử dụng khác payload.

### 8. P2 — Script quality gate tìm sai Maven wrapper trên Windows

- `scripts/verify-local.py:24` chỉ resolve file tương đối khi tên bắt đầu bằng `./`; nhánh Windows tại dòng 54 chọn `mvnw.cmd`.
- Chạy gate từ root báo `missing executable: mvnw.cmd`, dù wrapper thực sự nằm trong `app/backend-service`.
- Chạy trực tiếp `app/backend-service/mvnw.cmd verify` đã đi tới test suite sau khi cho phép ghi Maven cache.
- Cần resolve wrapper theo `Step.cwd` hoặc dùng absolute path; không coi báo missing này là wrapper bị xóa.

## Clean code và migration

### Ranh giới backend chưa sạch

Backend architecture tests phát hiện API DTO phụ thuộc outbound repository records. Ví dụ `app/backend-service/src/main/java/com/narrativex/backend/feature/generation/api/response/ChapterContinuityResponse.java:3` import `ChapterContinuityRepository.CurrentContinuity`. Nên trả application query/view từ inbound use case rồi map thành API response. ArchUnit báo 37 dependency edges vi phạm; đây không phải 37 bug độc lập.

Ngoài ra có application use case gọi/import use case của feature khác. Cần giữ boundary qua inbound port rõ ràng thay vì làm sạch bằng cách nới architecture test.

### UI và logic: đã tách một phần

- Đã có `api / queries / model / components / screens`; editor tách explorer/inspector/preview/timeline, Chapter và Voice có các panel riêng. Native operations đi qua typed preload. Token màu/surface tập trung trong `styles.css`.
- `StoryboardScreen.tsx` còn 788 dòng, trong đó `runGeminiQueue` tại dòng 349 phối hợp reconciliation, parallel execution, attempt transitions và persist queue. Screen orchestration được tài liệu cho phép; đây là ứng viên refactor để dễ kiểm thử/bảo trì, không tự động là vi phạm chỉ vì dài.
- `SettingsScreen.tsx:22` và `components/GeminiBrowserSettings.tsx:30` vẫn trộn IPC/lifecycle/status với rendering. Có thể tách hook/controller; ưu tiên sau các lỗi correctness.
- Test Desktop có nhiều kiểm tra source bằng regex/import function; 376 test pass vẫn không bắt lỗi JSX. Những test này hữu ích để guard kiến trúc nhưng không thay thế compiler và actual interaction tests.

### Field/file/schema cần quyết định trước khi xóa

- `V4__narration_notifications_and_artifacts.sql:65` còn `narration_sets`, `narration_documents`, `narration_document_chapters`, `narration_alignment_runs`; không tìm thấy runtime producer trong phạm vi đã tìm. `MediaPlanMapper.xml:38,46` vẫn insert FK nullable liên quan. Đây là ứng viên di sản/tính năng chưa nối, chưa chứng minh có thể xóa an toàn khỏi DB thật.
- Worker `narration/repository/claims.py` và `operations.py` chủ yếu re-export implementation; không tìm thấy runtime consumer của hai module này. Cần kiểm tra public import compatibility trước khi dọn.
- Worker `config.py:50` còn `backend_url` không có runtime reader được tìm thấy; có thể là cấu hình dư sau migration sang PostgreSQL polling.
- Desktop preload `types.ts:84` trả `workspacePath` qua catalog IPC dù renderer không dùng. Main còn dùng path nội bộ; nên tách DTO public khỏi internal catalog entry, giữ path trong main. Không kết luận path này đang bị gửi lên backend.
- `styles.css:74` khai báo `--voice-card` rồi dòng 97 override cùng token; không tìm thấy consumer ngoài stylesheet trong source đã tìm. Có thể dọn token/alias dựa trên usage, không xóa hàng loạt theo tên “legacy”.

### Tài liệu migration còn lệch

- `documentation/architecture/flyway-baseline-policy.md:9` chỉ liệt kê V1–V8, trong khi baseline thực tế và `DATABASE_BASELINE.md` đã là V1–V14. README cũng còn phát biểu V1–V8. Docs drift script chưa bắt lệch này.
- ADR-0022 nói R2 chỉ ACCOUNT voice, nhưng runtime hiện có SYSTEM catalog voice dùng R2. Cần cập nhật rõ phạm vi quyết định; không đồng nhất SYSTEM voice với project media.
- Những finding cũ đã được sửa: `short_clip_requests` đã bị bỏ; VisualBeat standalone camera/audio fields đã migrate sang structured direction. Không tiếp tục liệt kê chúng là code thừa hiện tại.

### Mock data

Không tìm thấy renderer runtime mock/demo data hoặc config chọn mock trong source đã rà soát. Local snapshot fallback là dữ liệu thật đã lưu, không phải mock.

Worker vẫn có FakeAnalysis/FakeImage/FakeTts trong `src`, có thể chọn bằng cấu hình development/staging/test; vì vậy không thể nói toàn repo không có fake code. Guard production/prod chặn fake cho enabled worker role, production Compose đặt production và real providers. Không có bằng chứng Compose production hiện đang trả fake success. Không coi fake adapter phục vụ test là code thừa mặc định.

## Verification

| Kiểm tra | Kết quả |
|---|---|
| Secret scan | PASS |
| Docs drift | PASS, vẫn bỏ sót inventory baseline |
| Repository gate từ root | FAIL: Windows wrapper discovery |
| Backend Maven verify trực tiếp | 453 tests: 5 failures, 1 error, 68 skipped; 379 passed |
| Desktop tests | 376 passed, 4 skipped |
| Desktop type-check | FAIL: JSX syntax |
| Desktop build chạy riêng | FAIL: cùng JSX syntax |
| Desktop dev | Main/preload build được; launch fail `Electron uninstall` |
| Worker pytest | 322 passed, 32 skipped |
| Worker Ruff | FAIL: 14 E501 trong tests |
| Worker format check bổ sung | 30 files cần format |
| Worker mypy mở rộng `src tests` | Dừng vì duplicate module fixture; không coi đây là kết quả src-only |
| Worker fault reproduction | Xác nhận partial-write recovery và MIME replay lỗi |

Backend failures cần phân biệt: architecture dependency là vấn đề thật; Flyway test cấm `camera_movement VARCHAR` trên toàn V2 bắt nhầm field của media plan snapshot; một prompt expectation thiếu nhãn `appearance:` mới; một Mockito test có unnecessary stubbing. Không suy ra tất cả test đỏ đều là runtime bug. Một prompt equality test cũng phát hiện hai đường format khác nhau: storyboard provider sanitize prompt cuối cùng, làm mất blank lines, còn Gemini adapter giữ format của composer.

Không có Docker command nên các integration test bị skip không được xác minh; không migrate/reset database thật. Không có screenshot/visual verification thành công và chưa thực hiện các flow editor/render/Gemini qua UI. Do đó không kết luận FE “đã ổn” từ unit tests.

Log lần chạy nằm trong `.tmp/audit-20260908-*.log`; backend có thêm `app/backend-service/target/surefire-reports`. Các log là artifact local, không phải test evidence từ CI.

## Thứ tự xử lý đề xuất

1. Sửa JSX, chạy tiếp type-check/build; sửa quality-gate wrapper và các test đang đỏ.
2. Khép entitlement export/watermark, atomic local storage và lock/reference race.
3. Sửa MIME replay, favorite snapshot, idempotent replay; thêm regression tests đúng các tình huống lỗi.
4. Đồng bộ migration policy, xác định ownership schema/field dư và tách các hook/controller còn trộn UI.
5. Chạy PostgreSQL integration và Desktop runtime verification trên môi trường có Electron/backend đầy đủ trước khi xác nhận hoàn tất.
